# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=rust_url_shortener
POSTGRES_PORT=5433

# Application Configuration
APP_PORT=8080

# Migration Configuration
RUN_MIGRATIONS=true

# [for setup.sh] Domain Configuration (for production)
# Set your custom domain here. This is only needed for production setup.
# Make sure to point your domain's A record to your server's IP address.
DOMAIN=example.com

# Caddy Port Configuration
# HTTP port for Caddy (default: 80)
CADDY_HTTP_PORT=80
# HTTPS port for Caddy (default: 443)
# Note: Ports 80 and 443 must be accessible for Let's Encrypt to work
CADDY_HTTPS_PORT=443
